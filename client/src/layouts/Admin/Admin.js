import React from 'react'
import Layout from '../Layout/Layout'

import routes from '../../utils/routes/adminRoutes'

const Admin= () => {
  return (
    <Layout routes={routes} />
  )
}

export default Admin 
